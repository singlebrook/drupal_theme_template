# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'compass' do
  watch(%r{scss/(.*)\.s[ac]ss})
end

guard 'livereload', :port => '35777' do
  watch(%r{.+\.(css|js)$})
end
